Step 1
Login into your github account, click on create a new repo
Give your repo the name "AWSLabs"
Ensure Public is selected 
Click create repository 

Step 2
Open VScode in your local machine, click on "terminal" to open a command line terminal.
If on Windows (it will have powershell by default), type wsl to change this  
Navigate to Desktop, create a folder with name "AWSLabs" using mkdir AWSLabs command
Navigate into the folder using cd AWSLabs
Run the command given on Github to initialize a local Git project and connect it to the repository

Run command git clone https://github.com/Nikiobilor/intro-to-aws-lab.git (this will clone this repo into your local machine)
Run command cd intro-to-aws-lab
Run command ls -la to view content of local repo
Copy Lab1.txt into the newly created folder "AWSLabs" using cp intro-to-aws-lab/Lab1.txt .
Delete the cloned repo

Step 3 (Ignore if you have already set up your github authentication)
Go to your Github account
Create a personal access token in your Github account to enable you push the repo into your Personal account
Kindly follow the steps in this link https://docs.github.com/en/enterprise-server@3.4/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token

Step 4
Run the necessary git command to enable you push Lab1.txt to your remote repo
git status, git add Lab1.txt, git commit -m "text" and then run git push since the upstream have been setup
Verify text file exits in your remote repo.

